
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: krampoline
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      
      apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: krmp-d2hub-idock.9rum.cc/dev-test/repo_a9e11c0e3efd:latest

---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: krampoline
spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080